import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  inputWrapper: {
    marginBottom: "0.9rem",
  },
  inputLabel: {
    fontSize: "0.8rem",
    color: "#25252d",
    marginBottom: "7px",
  },
}));
